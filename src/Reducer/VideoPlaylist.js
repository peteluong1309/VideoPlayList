const iniState = {
    videoList : []
};

const videoPlaylist = (state = iniState, action) => {
    switch(action.type){
        default: 
            return state;
    }
};

export default videoPlaylist;